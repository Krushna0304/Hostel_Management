# Production environment configurations
app.cors.allowed-origins=${FRONTEND_URL:https://hostel-management-dashboard.onrender.com}

# Set MONGODB_URI in the production environment with the current connection
# string copied from MongoDB Atlas. Do not fall back to a hard-coded cluster
# name: a deleted/renamed Atlas cluster otherwise causes continuous DNS errors.
# spring.data.mongodb.uri=${MONGODB_URI}
